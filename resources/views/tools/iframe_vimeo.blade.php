<iframe
        src="https://player.vimeo.com/video/{{ $video_code }}"
        width="{{ $width }}"
        height="{{ $height }}"
        frameborder="0"
        allow="autoplay; fullscreen"
        allowfullscreen>
</iframe>